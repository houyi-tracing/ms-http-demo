apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: SERVICE_NAME
spec:
  http:
      fault:
        abort:
          percentage:
            value: ABORT_PERCENTAGE
          httpStatus: 500
---
